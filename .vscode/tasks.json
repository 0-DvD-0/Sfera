{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "FilterCobalto",
            "type": "shell",
            "command": "cd build;./FilterCobalto ${input:targetFile} ${input:targetFilter} && python3 ../TelegramNotifications.py 'Il file: ${input:targetFile} è stato convertito' da FilterCobalto||  python3 ../TelegramNotifications.py 'Il file: ${input:targetFile} non è stato convertito'",
            "problemMatcher": [],
            "options": {
                "statusbar": {
                  "hide" : false,
                  "label": "FilterCobalto $(zap)"
                }
            }
        }
    ],
    "inputs": [
        {
            "id": "targetFile",
            "type": "command",
            "command": "extension.commandvariable.file.pickFile",
            "args": {
                "include": "**/*.{dat}"
            }

        },
        {   
            "id": "targetFilter",
            "type": "command",
            "command": "extension.commandvariable.file.pickFile",
            "args": {
                "include": "**/*.{csv}"
            }
        }
    ] 
}
